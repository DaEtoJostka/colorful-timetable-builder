module.exports = function(grunt) {
  // Project configuration
  grunt.initConfig({
    pkg: grunt.file.readJSON('package.json'),
    // Add your grunt tasks here
  });

  // Load plugins here
  
  // Default task(s)
  grunt.registerTask('default', []);
}; 